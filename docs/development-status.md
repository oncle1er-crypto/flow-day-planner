# Development status

The production reference branch is `main`. Changes are delivered through reviewed `chatgpt/**`
branches and pull requests.

The repository has automated typechecking, linting, unit tests, connected browser E2E tests,
production E2E tests, dependency auditing, Android builds, and iOS simulator builds. Remaining
release evidence is tracked in `production-checklist.md` rather than in a stale branch name.
